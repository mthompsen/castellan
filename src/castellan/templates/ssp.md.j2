# System Security Plan: {{ system.name }}

*Generated by Castellan on {{ generated }}. This is an SSP skeleton: control
implementation narratives must be completed by the system owner.*

## 1. System Identification

| Field | Value |
| --- | --- |
| System name | {{ system.name }} |
| System identifier | {{ system.system_id }} |
| System owner | {{ system.owner }} |
| Overall categorization | {{ cat.overall }} |
| Selected baseline | SP 800-53B {{ cat.selected_baseline }} ({{ control_count }} controls) |

## 2. System Description

{{ system.description.strip() }}

### Components

{% for component in system.components %}
- {{ component }}
{% endfor %}

## 3. FIPS-199 Security Categorization

{% if system.information_types %}
| Information Type | Confidentiality | Integrity | Availability |
| --- | --- | --- | --- |
{% for info in system.information_types %}
| {{ info.name }} | {{ info.confidentiality }} | {{ info.integrity }} | {{ info.availability }} |
{% endfor %}

{% endif %}
| Security Objective | Impact Level |
| --- | --- |
| Confidentiality | {{ cat.confidentiality }} |
| Integrity | {{ cat.integrity }} |
| Availability | {{ cat.availability }} |

**Rationale:** per FIPS-199, the overall categorization is the high-water mark
across the three security objectives
{%- if system.information_types %} (each objective itself the high-water mark
across the system's information types){% endif %}:
confidentiality = {{ cat.confidentiality }}, integrity = {{ cat.integrity }},
availability = {{ cat.availability }} → **{{ cat.overall }}**. Per FIPS-200,
this selects the SP 800-53B **{{ cat.selected_baseline }}** baseline.

## 4. Control Implementation

{% for family in families %}
### {{ family.code }} — {{ family.title }}

{% for control in family.controls %}
#### {{ control.id | control_id }} — {{ control.title }}

{% if control.statement %}
> {{ control.statement | replace("\n", "\n> ") }}
{% endif %}

- **Implementation status:** {{ statuses[control.id] }}
- **Responsible role:** _to be assigned_
- **Implementation narrative:** _to be completed_

{% endfor %}
{% endfor %}
